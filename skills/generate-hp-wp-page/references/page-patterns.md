# Page Structure Patterns

*Auto-generated from 57 published pages/posts on the live WordPress site.*
*Do not edit manually — regenerate with `npm run sync-wp`.*

---

## Real Page Blueprints

These are the actual module sequences used on published pages. Use them as reference when planning new pages.

### Pages

**Agenturen** (`/agenturen`):
`subheader`

**Smart-Modulbaukasten** (`/smart-modulbaukasten`):
`subheader → textbadges → stickycolumn → stepsscroll(bg-primary) → cardsfull → accordion(bg-light-yellow) → contactextended(bg-light-yellow) → teaserboxes → latestposts(bg-light-yellow)`

**WordPress Entwicklungspartner für Agenturen** (`/wordpress-entwicklungspartner-fuer-agenturen`):
`subheader → texttext → textbadges → cardsextended(bg-light-yellow) → quotereveal(bg-light-yellow) → stepsscroll(bg-primary) → cardsfull → video → contactextended(bg-light-yellow) → accordion → teaserboxes → latestposts(bg-light-yellow)`

**Website-Relaunch-Agentur** (`/website-relaunch-agentur`):
`subheader → textbadges(bg-light-yellow) → quotereveal(bg-light-yellow) → textmodule → cardsanimated → texttext → stepsscroll(bg-primary-green) → cardsfull → casesingle → teaserboxes → accordion(bg-light-yellow) → contactextended(bg-light-yellow) → teaserboxes → latestposts(bg-light-yellow)`

**Screendesign Figma Template** (`/screendesign-figma-template`):
`subheader`

**Unsere WordPress-Philosophie** (`/unsere-wordpress-philosophie`):
`subheader`

**Bundles** (`/bundles`):
`subheader`

**WordPress Agentur** (`/wordpress-agentur`):
`subheader`

**Webdesign Agentur** (`/webdesign-agentur`):
`subheader → textbadges(bg-light-yellow) → textimage → stepsscroll(bg-light-yellow) → texttext(bg-primary) → cards → textmodule(bg-primary-green) → textbullets → accordion(bg-light-yellow) → contactextended(bg-light-yellow)`

**Technisches SEO** (`/technische-seo-optimierung`):
`subheader`

**SEO Strategie** (`/seo-strategie`):
`subheader`

**SEO Content Agentur** (`/seo-content`):
`subheader`

**SEO Beratung** (`/seo-beratung`):
`subheader`

**WordPress Wartung** (`/wordpress-wartung-service`):
`subheader`

**WordPress Entwicklung** (`/wordpress-entwicklung`):
`subheader`

**Website Konzeption & Design** (`/website-konzeption-design`):
`subheader`

**SEO** (`/seo`):
`subheader`

**White Label Agentur** (`/white-label-agentur`):
`subheader`

**Webdesign für Zahnärzte** (`/webdesign-fuer-zahnaerzte`):
`subheader`

**Webdesign für Vereine** (`/webdesign-fuer-vereine`):
`subheader`

**Webdesign für Bauunternehmen** (`/webdesign-fuer-bauunternehmen`):
`subheader`

**B2B Website** (`/webdesign-b2b`):
`subheader`

**Handwerker Website** (`/handwerker-website`):
`subheader`

**WordPress Agentur München** (`/wordpress-agentur-muenchen`):
`subheader`

**WordPress Agentur Köln** (`/wordpress-agentur-koeln`):
`subheader`

**Insights** (`/insights`):
`subheader`

**Partner** (`/partner`):
`subheader`

**Leistungen** (`/leistungen`):
`subheader`

**Branchen** (`/branchen`):
`subheader`

**Datenschutz** (`/datenschutz`):
`subheader`

**Impressum** (`/impressum`):
`subheader`

**Home** (`/home`):
`homeheader → postsslider(bg-primary) → logoslider → cardsanimated → quotereveal → cards → stepsscroll → image(bg-light-yellow) → quicklinks(bg-light-yellow) → contactextended(bg-primary-dark) → latestposts → textimage`

### Insights (Blog Posts)

**User-Rollen-Konzepte**:
`textmodule → textmodule → textmodule → textmodule → endsidecontent → contactextended(bg-primary-green) → latestposts`

**Was ist ein technischer Relaunch?**:
`textmodule → textmodule → textimage → accordion → endsidecontent → contactextended(bg-primary-green) → textmodule → latestposts`

**Relaunch: Brauche ich ein Archiv für die alte Website?**:
`textmodule → textmodule → textmodule → image → textmodule → endsidecontent → contactextended(bg-primary-green) → teaserboxes → latestposts`

**Was ist SSL und HTTPS?**:
`textmodule → textmodule → image → textmodule → endsidecontent → contactextended(bg-primary-green) → latestposts`

**Warum brauchen WordPress-Seiten regelmäßige Updates?**:
`textmodule → textmodule → textmodule → image → textmodule → gallerywallsimple(bg-primary-green) → textmodule → endsidecontent → contactextended(bg-primary-green) → accordion → image → accordion → latestposts(bg-primary-green)`

### Cases

**IVF Dresden SEO-Betreuung**:
`subheader`

**Internationaler Controllerverein Relaunch**:
`subheader`

**Kulturzelt Wolfshagen Relaunch**:
`subheader`

**Webentwicklung für Holzhausfabrik**:
`subheader`

**Webentwicklung für Ziro**:
`subheader`

---

## Block Usage Frequency

| Block | Usage Count |
| :--- | :--- |
| `acf/subheader` | 49 |
| `acf/textmodule` | 28 |
| `acf/accordion` | 13 |
| `acf/contactextended` | 13 |
| `acf/latestposts` | 12 |
| `acf/image` | 11 |
| `acf/endsidecontent` | 7 |
| `acf/stepsscroll` | 6 |
| `acf/teaserboxes` | 6 |
| `acf/textbadges` | 5 |
| `acf/texttext` | 4 |
| `acf/quotereveal` | 4 |
| `acf/cardsfull` | 3 |
| `acf/cardsanimated` | 3 |
| `acf/textimage` | 3 |
| `acf/cards` | 2 |
| `acf/textbullets` | 2 |
| `acf/gallerywallsimple` | 2 |
| `acf/stickycolumn` | 1 |
| `acf/cardsextended` | 1 |
| `acf/video` | 1 |
| `acf/casesingle` | 1 |
| `acf/homeheader` | 1 |
| `acf/postsslider` | 1 |
| `acf/logoslider` | 1 |
| `acf/quicklinks` | 1 |

## Common Block Adjacency Patterns

These pairs appear frequently together in sequence:

| Pattern | Occurrences |
| :--- | :--- |
| `acf/textmodule → acf/textmodule` | 10 |
| `acf/endsidecontent → acf/contactextended` | 7 |
| `acf/teaserboxes → acf/latestposts` | 5 |
| `acf/textmodule → acf/endsidecontent` | 5 |
| `acf/contactextended → acf/teaserboxes` | 4 |
| `acf/contactextended → acf/latestposts` | 4 |
| `acf/textmodule → acf/image` | 4 |
| `acf/image → acf/textmodule` | 4 |
| `acf/image → acf/accordion` | 4 |
| `acf/subheader → acf/textbadges` | 3 |
| `acf/stepsscroll → acf/cardsfull` | 3 |
| `acf/accordion → acf/contactextended` | 3 |
| `acf/accordion → acf/image` | 3 |
| `acf/subheader → acf/texttext` | 2 |
| `acf/contactextended → acf/accordion` | 2 |

## Observed Design Rules

Based on analysis of real pages:

- **Contact placement**: The `acf/contactextended` module is consistently placed near the end of pages, typically as the second-to-last or third-to-last block.
- **LatestPosts as closer**: `acf/latestposts` frequently appears as the final block on pages.
- **Page opening**: Pages use `acf/homeheader` (homepage) or `acf/subheader` (inner pages) as the first block.
- **Visual breaks**: `acf/divider`, `acf/quotereveal`, `acf/badges` and `acf/cardsanimated` are used to break up sequences of content-heavy modules.
- **Color alternation**: Consecutive blocks rarely share the same non-default background color.
